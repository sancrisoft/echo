# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Xcode — build output
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
# (Package.resolved is kept in xcshareddata so dependency versions are pinned)
.build/
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata

# Accidentally generated
*.swp

# local models
.local-models

# SP-002 measurement diagnostic (regenerated by ExternalInputMeasurementTests)
EchoTests/sp002-measure-report.txt